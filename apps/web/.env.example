NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_APP_NAME=VYOM Publication

# Set to true to enable the demo authentication system (hardcoded credentials,
# localStorage session). Must be false or unset in production — real JWT auth
# is used instead.
NEXT_PUBLIC_DEMO_MODE=true
